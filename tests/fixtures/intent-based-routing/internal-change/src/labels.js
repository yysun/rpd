/*
 * Feature: Render internal labels.
 * Implementation: Return the supplied label for internal callers.
 * Recent changes: Seeded direct-change fixture.
 */

export function renderLabel(value) {
  return value;
}
